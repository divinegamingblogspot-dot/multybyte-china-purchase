const AUTH_API_URL='https://script.google.com/macros/s/AKfycbwDHq7TB9vSxTlSeG9i55HyuhPzAP8oryRWdLsKhPZVHQubgdIAs8CJ7sknQTJLmabj/exec';
const PREVIEW_MODE=new URLSearchParams(location.search).get('preview')==='1';
const AUTO_REFRESH_MS=15000,REQUEST_TIMEOUT_MS=60000,SESSION_KEY='mb_vendor_session',CACHE_KEY='mb_portal_cache';
let allProducts=[],lastUpdated='',loading=false,requestTimer=null,loginMode='vendor',dense=false;
const $=id=>document.getElementById(id),hide=id=>$(id).classList.add('hidden'),show=id=>$(id).classList.remove('hidden');
function jsonp(params){return new Promise((resolve,reject)=>{const cb='mbCallback_'+Date.now()+'_'+Math.random().toString(36).slice(2),script=document.createElement('script');let done=false;const timer=setTimeout(()=>{cleanup();reject(new Error('Secure data stream timed out.'))},REQUEST_TIMEOUT_MS);function cleanup(){if(done)return;done=true;clearTimeout(timer);delete window[cb];script.remove()}window[cb]=d=>{cleanup();resolve(d)};script.onload=()=>setTimeout(()=>{if(!done){cleanup();reject(new Error('Gateway responded without a callback. Check the Apps Script deployment.'))}},1200);script.onerror=()=>{cleanup();reject(new Error('Gateway unavailable. Check the Apps Script Web App deployment and access setting.'))};script.async=true;script.src=AUTH_API_URL+'?'+new URLSearchParams({...params,callback:cb,_:Date.now()});document.head.appendChild(script)})}
function setConnection(state,text){const b=$('connectionBadge');b.className='connection-badge '+state;b.innerHTML='<i></i> '+text}
function setLoginMode(m){loginMode=m;$('loginTitle').textContent=m==='admin'?'Administrator sign in':'Vendor sign in';$('loginSubtitle').textContent=m==='admin'?'Enter the command layer to manage vendors and MASTER data.':'Authenticate to access your assigned purchase intelligence.';$('loginId').placeholder=m==='admin'?'Admin ID':'User ID';$('adminModeBtn').textContent=m==='admin'?'← Back to vendor access':'Switch to admin access';hide('loginError')}
function showLogin(){hide('adminView');hide('portalView');show('loginView');hide('refreshBtn');hide('logoutBtn');hide('vendorBadge');setConnection('','GATEWAY READY')}
function showPortal(name){hide('loginView');hide('adminView');show('portalView');show('refreshBtn');show('logoutBtn');show('vendorBadge');$('vendorBadge').textContent=name;$('portalTitle').textContent=name+' // Products';loadVendorData()}
function showAdmin(){hide('loginView');hide('portalView');show('adminView');show('logoutBtn');hide('refreshBtn');show('vendorBadge');$('vendorBadge').textContent='ADMIN COMMAND';loadVendors()}
function saveSession(s){localStorage.setItem(SESSION_KEY,JSON.stringify(s))}function getSession(){try{return JSON.parse(localStorage.getItem(SESSION_KEY)||'null')}catch(e){return null}}
function cacheData(){try{localStorage.setItem(CACHE_KEY,JSON.stringify({data:allProducts,updated:lastUpdated,at:Date.now()}))}catch(e){}}
function restoreCache(){try{const c=JSON.parse(localStorage.getItem(CACHE_KEY)||'null');if(c?.data?.length){allProducts=c.data;lastUpdated=c.updated||'';render();updateMetrics();$('statusText').textContent='Cached snapshot · awaiting live sync'}}catch(e){}}
function clearSession(){localStorage.removeItem(SESSION_KEY);allProducts=[];lastUpdated='';$('products').replaceChildren()}
async function login(e){e.preventDefault();const id=$('loginId').value.trim(),password=$('loginPassword').value,err=$('loginError'),btn=$('loginSubmit');hide('loginError');if(!id||!password)return;btn.disabled=true;btn.classList.add('busy');setConnection('sync','AUTHENTICATING');try{const r=await jsonp({action:'login',role:loginMode,id,password});if(!r||!r.success)throw Error(r?.message||'Invalid credentials.');saveSession({token:r.token,role:r.role,id:r.id,name:r.name});$('loginPassword').value='';setConnection('online','SECURE');loginMode==='admin'?showAdmin():showPortal(r.name||r.id)}catch(x){err.textContent=x?.message||'Connection problem.';show('loginError');setConnection('error','AUTH FAILED')}finally{btn.disabled=false;btn.classList.remove('busy')}}
async function loadVendorData(){const s=getSession();if(!s||s.role!=='vendor'){showLogin();return}if(loading)return;loading=true;setConnection('sync','SYNCING');show('loading');hide('error');try{const r=await jsonp({action:'vendorData',token:s.token});if(!r||!r.success)throw Error(r?.message||'Session expired.');const changed=r.updated!==lastUpdated||!allProducts.length;if(changed){allProducts=(r.data||[]).filter(p=>p&&(p.sku||p.productName)).map(normalizeProduct);lastUpdated=r.updated||'';cacheData();render();updateMetrics()}$('statusText').textContent=r.updated?`Live sync · ${new Date(r.updated).toLocaleString()}`:'Live data synchronized';hide('loading');hide('error');setConnection('online','LIVE')}catch(e){if(/Session|token/i.test(e.message)){clearSession();showLogin()}else{hide('loading');show('error');$('error').textContent=e.message;setConnection('error','OFFLINE');restoreCache()}}finally{loading=false}}
async function loadPreview(){try{const r=await fetch('data.json?preview='+Date.now(),{cache:'no-store'});const d=await r.json();allProducts=(d.data||[]).filter(p=>p&&p.sku).map(normalizeProduct);lastUpdated=d.updated||'';render();updateMetrics();$('statusText').textContent='Local preview snapshot';hide('loading');setConnection('online','PREVIEW')}catch(e){showError('Preview data could not be loaded.')}}
function normalizeProduct(p){return{...p,image:p.image||'',productLink:normalizeUrl(p.productLink)}}function normalizeUrl(v){if(!v)return'';const u=String(v).trim().replace(/\\u0026/g,'&').replace(/&amp;/g,'&');return/^https?:\/\//i.test(u)?u:''}function showError(m){hide('loading');show('error');$('error').textContent=m||'Connection problem'}
function filteredProducts(){const q=$('searchInput').value.trim().toLowerCase(),sort=$('sortSelect').value;let list=allProducts.filter(p=>[p.sku,p.productName,p.supplier,p.quantity,p.price,p.remarks].join(' ').toLowerCase().includes(q));const val=x=>String(x??'').toLowerCase();if(sort==='sku')list.sort((a,b)=>val(a.sku).localeCompare(val(b.sku)));if(sort==='name')list.sort((a,b)=>val(a.productName).localeCompare(val(b.productName)));if(sort==='supplier')list.sort((a,b)=>val(a.supplier).localeCompare(val(b.supplier)));if(sort==='quantity')list.sort((a,b)=>(parseFloat(b.quantity)||0)-(parseFloat(a.quantity)||0));if(sort==='price')list.sort((a,b)=>(parseFloat(a.price)||0)-(parseFloat(b.price)||0));return list}
function render(){const list=filteredProducts(),frag=document.createDocumentFragment(),t=$('productTemplate');list.forEach(p=>{const n=t.content.cloneNode(true),wrap=n.querySelector('.image-wrap'),img=n.querySelector('.product-image'),a=n.querySelector('.product-link');n.querySelector('.sku').textContent=p.sku||'NO SKU';n.querySelector('.product-name').textContent=p.productName||'Product';n.querySelector('.quantity').textContent=p.quantity||'—';n.querySelector('.supplier').textContent=p.supplier||'—';n.querySelector('.price').textContent=p.price??'—';n.querySelector('.remarks').textContent=p.remarks??'—';if(p.image){img.src=p.image+(p.image.includes('?')?'&':'?')+'v='+encodeURIComponent(p.sku||'1');img.onerror=()=>{img.removeAttribute('src');wrap.classList.add('no-photo')}}else wrap.classList.add('no-photo');if(p.productLink)a.href=p.productLink;else{a.removeAttribute('href');a.textContent='No product link';a.style.opacity='.45'}frag.appendChild(n)});$('products').replaceChildren(frag);$('countBadge').textContent=`${list.length} visible / ${allProducts.length} total`;list.length?hide('empty'):show('empty')}
function updateMetrics(){const suppliers=new Set(allProducts.map(p=>p.supplier).filter(Boolean));$('metricSkus').textContent=allProducts.length;$('metricSuppliers').textContent=suppliers.size;$('metricSync').textContent=lastUpdated?new Date(lastUpdated).toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'}):'—'}
async function loadVendors(){const s=getSession();if(!s||s.role!=='admin'){showLogin();return}const box=$('vendorList');box.innerHTML='<div class="state">Scanning identity nodes…</div>';try{const r=await jsonp({action:'adminVendors',token:s.token});if(!r.success)throw Error(r.message||'Unable to load users.');box.replaceChildren();(r.vendors||[]).forEach(v=>{const d=document.createElement('div');d.className='vendor-row';d.innerHTML=`<div class="node-avatar">${esc((v.name||v.id).slice(0,2).toUpperCase())}</div><div><strong>${esc(v.name)}</strong><small>${esc(v.id)} · ${esc(v.sheetName)}</small></div><span class="status ${v.enabled?'on':'off'}">${v.enabled?'ONLINE':'DISABLED'}</span><button class="mini-btn">Edit</button>`;d.querySelector('button').onclick=()=>editVendor(v);box.appendChild(d)});if(!r.vendors?.length)box.innerHTML='<div class="state">No vendor identities yet.</div>'}catch(e){box.innerHTML=`<div class="state error">${esc(e.message)}</div>`}}
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function editVendor(v){$('vendorEditId').value=v.id;$('vendorIdInput').value=v.id;$('vendorIdInput').disabled=true;$('vendorNameInput').value=v.name;$('vendorPasswordInput').value='';$('vendorSheetInput').value=v.sheetName;$('vendorEnabledInput').checked=!!v.enabled;show('vendorCancelBtn')}
async function saveVendor(e){e.preventDefault();const s=getSession(),msg=$('vendorFormMsg'),payload={action:'saveVendor',token:s.token,id:$('vendorIdInput').value.trim(),name:$('vendorNameInput').value.trim(),password:$('vendorPasswordInput').value,sheetName:$('vendorSheetInput').value.trim(),enabled:$('vendorEnabledInput').checked};msg.textContent='Committing…';try{const r=await jsonp(payload);if(!r.success)throw Error(r.message||'Save failed.');msg.textContent='Identity committed.';resetVendorForm();loadVendors()}catch(x){msg.textContent=x.message}}
function resetVendorForm(){$('vendorForm').reset();$('vendorEditId').value='';$('vendorIdInput').disabled=false;$('vendorEnabledInput').checked=true;hide('vendorCancelBtn')}
function logout(){clearSession();$('loginId').value='';$('loginPassword').value='';showLogin()}
function toggleTheme(){document.body.classList.toggle('light-mode');localStorage.setItem('mb_theme',document.body.classList.contains('light-mode')?'light':'dark')}
function toggleDensity(){dense=!dense;document.body.classList.toggle('dense',dense);localStorage.setItem('mb_dense',dense?'1':'0')}
function openCommand(){show('commandPalette');$('commandInput').value='';setTimeout(()=>$('commandInput').focus(),30)}function closeCommand(){hide('commandPalette')}
function runCommand(c){if(c==='refresh')loadVendorData();if(c==='focus'){$('searchInput').focus();closeCommand()}if(c==='density')toggleDensity();if(c==='theme')toggleTheme();if(c==='logout')logout();if(c!=='focus')closeCommand()}
$('loginForm').addEventListener('submit',login);$('adminModeBtn').onclick=()=>setLoginMode(loginMode==='admin'?'vendor':'admin');$('logoutBtn').onclick=logout;$('adminLogoutBtn').onclick=logout;$('refreshBtn').onclick=loadVendorData;$('adminRefreshBtn').onclick=loadVendors;$('vendorForm').addEventListener('submit',saveVendor);$('vendorCancelBtn').onclick=resetVendorForm;$('searchInput').addEventListener('input',()=>{clearTimeout(requestTimer);requestTimer=setTimeout(render,40)});$('sortSelect').addEventListener('change',render);$('clearBtn').onclick=()=>{$('searchInput').value='';render();$('searchInput').focus()};$('themeBtn').onclick=toggleTheme;$('densityBtn').onclick=toggleDensity;$('commandBtn').onclick=openCommand;$('closeCommand').onclick=closeCommand;$('commandPalette').onclick=e=>{if(e.target.id==='commandPalette')closeCommand()};document.querySelectorAll('[data-cmd]').forEach(b=>b.onclick=()=>runCommand(b.dataset.cmd));$('commandInput').onkeydown=e=>{if(e.key==='Enter'){const q=e.target.value.toLowerCase();const hit=['refresh','focus','density','theme','logout'].find(x=>x.includes(q));if(hit)runCommand(hit)}};$('showPasswordBtn').onclick=()=>{$('loginPassword').type=$('loginPassword').type==='password'?'text':'password'};document.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();openCommand()}if(e.key==='Escape')closeCommand()});setInterval(()=>{$('footerClock').textContent=' · '+new Date().toLocaleTimeString()},1000);setInterval(()=>{if(!$('portalView').classList.contains('hidden'))loadVendorData()},AUTO_REFRESH_MS);
if(localStorage.getItem('mb_theme')==='light')document.body.classList.add('light-mode');if(localStorage.getItem('mb_dense')==='1'){dense=true;document.body.classList.add('dense')}
if(PREVIEW_MODE){showPortal('Preview Mode');loadPreview()}else{const s=getSession();if(s?.role==='admin')showAdmin();else if(s?.role==='vendor')showPortal(s.name);else showLogin()}
