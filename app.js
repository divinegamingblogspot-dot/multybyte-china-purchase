const AUTH_API_URL='https://script.google.com/macros/s/AKfycbwDHq7TB9vSxTlSeG9i55HyuhPzAP8oryRWdLsKhPZVHQubgdIAs8CJ7sknQTJLmabj/exec';
const PREVIEW_MODE=new URLSearchParams(location.search).get('preview')==='1';
const AUTO_REFRESH_MS=15000;
const REQUEST_TIMEOUT_MS=20000;
let allProducts=[],lastUpdated='',loading=false,requestTimer=null,loginMode='vendor';
const $=id=>document.getElementById(id),hide=id=>$(id).classList.add('hidden'),show=id=>$(id).classList.remove('hidden');
function jsonp(params){return new Promise((resolve,reject)=>{const cb='mbCallback_'+Date.now()+'_'+Math.random().toString(36).slice(2),script=document.createElement('script');let done=false;const timer=setTimeout(()=>{cleanup();reject(new Error('Server timeout. Apps Script did not return a JSONP response within 20 seconds.'))},REQUEST_TIMEOUT_MS);function cleanup(){if(done)return;done=true;clearTimeout(timer);delete window[cb];script.remove()}window[cb]=d=>{cleanup();resolve(d)};script.onload=()=>{setTimeout(()=>{if(!done){cleanup();reject(new Error('Apps Script loaded but did not execute the JSONP callback. The deployed Web App is not returning the expected JavaScript response.'))}},250)};script.onerror=()=>{cleanup();reject(new Error('Apps Script Web App could not be loaded. Check that the deployment is active and accessible to anyone.'))};const q=new URLSearchParams({...params,callback:cb,_:Date.now()});script.async=true;script.src=AUTH_API_URL+'?'+q.toString();document.head.appendChild(script)})}
function setLoginMode(m){loginMode=m;$('loginTitle').textContent=m==='admin'?'Admin sign in':'Vendor sign in';$('loginSubtitle').textContent=m==='admin'?'Manage users and access.':'Sign in to access your assigned products.';$('loginId').placeholder=m==='admin'?'Admin ID':'User ID';$('adminModeBtn').textContent=m==='admin'?'User login':'Admin login';hide('loginError')}
function showLogin(){hide('adminView');hide('portalView');show('loginView');hide('refreshBtn');hide('logoutBtn');hide('vendorBadge')}
function showPortal(name){hide('loginView');hide('adminView');show('portalView');show('refreshBtn');show('logoutBtn');show('vendorBadge');$('vendorBadge').textContent=name;loadVendorData()}
function showAdmin(){hide('loginView');hide('portalView');show('adminView');show('logoutBtn');hide('refreshBtn');show('vendorBadge');$('vendorBadge').textContent='ADMIN';loadVendors()}
function saveSession(s){localStorage.setItem('mb_vendor_session',JSON.stringify(s))}function getSession(){try{return JSON.parse(localStorage.getItem('mb_vendor_session')||'null')}catch(e){return null}}function clearSession(){localStorage.removeItem('mb_vendor_session');allProducts=[];lastUpdated='';$('products').replaceChildren()}
async function login(e){e.preventDefault();const id=$('loginId').value.trim(),password=$('loginPassword').value,err=$('loginError');hide('loginError');if(!id||!password)return;try{const r=await jsonp({action:'login',role:loginMode,id,password});if(!r||!r.success)throw Error(r&&r.message||'Invalid ID or password.');saveSession({token:r.token,role:r.role,id:r.id,name:r.name});$('loginPassword').value='';loginMode==='admin'?showAdmin():showPortal(r.name||r.id)}catch(x){err.textContent=x&&x.message||'Connection problem. Please try again.';show('loginError')}}
async function loadVendorData(){const s=getSession();if(!s||s.role!=='vendor'){showLogin();return}if(loading)return;loading=true;show('loading');hide('error');try{const r=await jsonp({action:'vendorData',token:s.token});if(!r||!r.success)throw Error(r&&r.message||'Session expired.');const changed=r.updated!==lastUpdated||!allProducts.length;if(changed){allProducts=(r.data||[]).filter(p=>p&&(p.sku||p.productName)).map(normalizeProduct);lastUpdated=r.updated||'';render()}$('countBadge').textContent=`${allProducts.length} item${allProducts.length===1?'':'s'}`;$('statusText').textContent=r.updated?`Updated ${new Date(r.updated).toLocaleString()}`:'Data synced';hide('loading');hide('error')}catch(e){if(/Session|token/i.test(e.message)){clearSession();showLogin()}else showError(e.message)}loading=false}
async function loadPreview(){try{const r=await fetch('data.json?preview='+Date.now(),{cache:'no-store'});const d=await r.json();allProducts=(d.data||[]).filter(p=>p&&p.sku).map(normalizeProduct);lastUpdated=d.updated||'';render();$('countBadge').textContent=`${allProducts.length} items`;$('statusText').textContent='Preview data';hide('loading')}catch(e){showError('Preview data could not be loaded.')}}
function normalizeProduct(p){return{...p,image:p.image||'',productLink:normalizeUrl(p.productLink)}}function normalizeUrl(v){if(!v)return'';let u=String(v).trim().replace(/\\u0026/g,'&').replace(/&amp;/g,'&');return/^https?:\/\//i.test(u)?u:''}function showError(m){hide('loading');show('error');$('error').textContent=m||'Connection problem'}
function render(){const q=$('searchInput').value.trim().toLowerCase(),list=allProducts.filter(p=>[p.sku,p.productName,p.supplier,p.quantity,p.price,p.remarks].join(' ').toLowerCase().includes(q));const frag=document.createDocumentFragment(),t=$('productTemplate');list.forEach(p=>{const n=t.content.cloneNode(true),wrap=n.querySelector('.image-wrap'),img=n.querySelector('.product-image'),a=n.querySelector('.product-link');n.querySelector('.sku').textContent=p.sku||'NO SKU';n.querySelector('.product-name').textContent=p.productName||'Product';n.querySelector('.quantity').textContent=p.quantity||'—';n.querySelector('.supplier').textContent=p.supplier||'—';n.querySelector('.price').textContent=p.price??'—';n.querySelector('.remarks').textContent=p.remarks??'—';if(p.image){img.src=p.image+(p.image.includes('?')?'&':'?')+'v='+encodeURIComponent(p.sku||'1');img.onerror=()=>{img.removeAttribute('src');wrap.classList.add('no-photo')}}else wrap.classList.add('no-photo');if(p.productLink)a.href=p.productLink;else{a.removeAttribute('href');a.textContent='No product link';a.style.opacity='.5'}frag.appendChild(n)});$('products').replaceChildren(frag);list.length?hide('empty'):show('empty')}
async function loadVendors(){const s=getSession();if(!s||s.role!=='admin'){showLogin();return}const box=$('vendorList');box.innerHTML='<div class="state">Loading users…</div>';try{const r=await jsonp({action:'adminVendors',token:s.token});if(!r.success)throw Error(r.message||'Unable to load users.');box.replaceChildren();(r.vendors||[]).forEach(v=>{const d=document.createElement('div');d.className='vendor-row';d.innerHTML=`<div><strong>${esc(v.name)}</strong><small>${esc(v.id)} · ${esc(v.sheetName)}</small></div><span class="status ${v.enabled?'on':'off'}">${v.enabled?'Enabled':'Disabled'}</span><button class="mini-btn">Edit</button>`;d.querySelector('button').onclick=()=>editVendor(v);box.appendChild(d)});if(!r.vendors?.length)box.innerHTML='<div class="state">No users yet.</div>'}catch(e){box.innerHTML=`<div class="state error">${esc(e.message)}</div>`}}
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function editVendor(v){$('vendorEditId').value=v.id;$('vendorIdInput').value=v.id;$('vendorIdInput').disabled=true;$('vendorNameInput').value=v.name;$('vendorPasswordInput').value='';$('vendorSheetInput').value=v.sheetName;$('vendorEnabledInput').checked=!!v.enabled;show('vendorCancelBtn')}
async function saveVendor(e){e.preventDefault();const s=getSession(),msg=$('vendorFormMsg'),payload={action:'saveVendor',token:s.token,id:$('vendorIdInput').value.trim(),name:$('vendorNameInput').value.trim(),password:$('vendorPasswordInput').value,sheetName:$('vendorSheetInput').value.trim(),enabled:$('vendorEnabledInput').checked};msg.textContent='Saving…';try{const r=await jsonp(payload);if(!r.success)throw Error(r.message||'Save failed.');msg.textContent='Saved successfully.';resetVendorForm();loadVendors()}catch(x){msg.textContent=x.message}}
function resetVendorForm(){$('vendorForm').reset();$('vendorEditId').value='';$('vendorIdInput').disabled=false;$('vendorEnabledInput').checked=true;hide('vendorCancelBtn')}function logout(){clearSession();$('loginId').value='';$('loginPassword').value='';showLogin()}
$('loginForm').addEventListener('submit',login);$('adminModeBtn').onclick=()=>setLoginMode(loginMode==='admin'?'vendor':'admin');$('logoutBtn').onclick=logout;$('adminLogoutBtn').onclick=logout;$('refreshBtn').onclick=loadVendorData;$('adminRefreshBtn').onclick=loadVendors;$('vendorForm').addEventListener('submit',saveVendor);$('vendorCancelBtn').onclick=resetVendorForm;$('searchInput').addEventListener('input',()=>{clearTimeout(requestTimer);requestTimer=setTimeout(render,40)});$('clearBtn').onclick=()=>{$('searchInput').value='';render();$('searchInput').focus()};setInterval(()=>{if(!$('portalView').classList.contains('hidden'))loadVendorData()},AUTO_REFRESH_MS);
if(PREVIEW_MODE){showPortal('Preview Mode');loadPreview()}else{const s=getSession();if(s?.role==='admin')showAdmin();else if(s?.role==='vendor')showPortal(s.name);else showLogin()}
